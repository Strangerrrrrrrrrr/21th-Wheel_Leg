- 本文针对：

灰度摄像头mt9v03x灰度摄像头（总钻风）的图像处理进行梳理；

对比英飞凌TC624芯片视觉处理全流程和CYT4bb7上的流程

图像算法绝大部分学习SJTU的RT1064图像处理方案

- 本文属于：

机器视觉（MV）范畴，涉及数字图像处理

（嵌入式c语法知识 数据结构与算法）

不包括：计算机视觉（CV）与AI网络模型交叉

（python、machine learning等）

- 核心处理芯片：CYT4bb7（1核）

视觉任务流程：  
图像采集->

边线检测+阈值分割处理->

标定计算角点，检测变化 ->

识别障碍物，切换运动状态

## 1. mt9v03x 采集图像

```c title:ALL.c

//TC264 逻辑
ips200_init();
# 屏幕初始化、
# 设置方向、颜色、字体

mt9v03_init();
parafalsh_init();  # 参数保存
mt9v03X_set_exposure_time(exposure);  # 动态调整曝光时间 
interrupt_global_enable(1);  #开启全局中断
```

TC264芯片当中摄像头通信支持UART和SCCB两种方式；

CYT4bb7芯片当中只支持SCCB通信协议；

（serial camera control bus ）

## 2. 图像预处理（阈值分割）

```c title:segmentation.c
//TC264图像处理方案

# 图像压缩
compressimage();

# 像素处理
// Stayguy_ADS(); 图像画线
Pixel_filter();

//摄像头采集： Mt9v03x_image[][]
//图像压缩后：Image_Use[][]
//阈值分割后：Pixel [][]
```

- 头文件定义时限制行、列数，避免溢出，越界访问

```
//CYT4bb采用了局部自适应阈值
//block_size = 7;
```

- 灰度处理算法：

1. 用双otsu阈值分割出三个区域，背景黑色，台阶白色，蓝色台阶为灰色；其余颜色阈值在蓝色附近统一处理成灰色
2. 自适应算法+迷宫法（左右手法则）：
3. 雷区边线粗细（10cm）
4. 单边桥宽度（45cm），需要搜出边线
5. 台阶宽度 (50cm)
6. ROI区域、阳光算法（梯度、算子等）：

- 大津法（otsu）：

一种图像分割的自动阈值选择算法，适用于图像灰度分布呈现”双峰“状态图片，采用最大类间方差的原理

![](https://cdn.nlark.com/yuque/0/2026/gif/61324111/1773836478652-36ed7cad-ea7c-4fc0-b2a7-935b7bab2ed6.gif)

- 活动轮廓模型：  
    计算量大、内存大。有别于遍历图像像素，该方法更像是几何拓扑的思想进行精细轮廓提取。属于图像分割的空域技术，通过能量函数计算内、外部能量的原理。[舍！]

【进阶：水平集方法 自动驾驶的语义分割】

## 3. 边线循迹（状态判断、切换）

采集到原始图像，先直接进行（开始main-loop）

1. 边线提取+处理【findedge】

左右手迷宫法+自适应阈值（计算量小还不用进行手动调阈值）  
Lmaze_Adapt（）  
Rmaze_Adapt（）

findedge（）

{

line

标定

滤波

采样

中线跟踪

}

2. 角点提取+筛选【findpoints】

角点检测（单边桥？台阶？障碍路段？）

3. 检查元素【RJTU：十字、三叉、圆环】
4. 处理中线、近点、远点（RJTU：353-450）
5. 绘制道路元素（线、锚点、角点）
6. 打印main-loop

## 4. 障碍物识别

### 4.1. 雷区检测

相机标定

逆透视变换

### 4.2. 台阶检测（跳跃障碍物）

当台阶的边线（蓝色边线）<设置好的图像行数

### 4.3. 单边桥检测

【最大疑惑】

20th： 最长白列识别角点>6

21th：相机标定、自适应阈值、角点检测（近处的精度高）

### 4.4. 大障碍物检测（颠簸路段）

## 5. 显示屏ips200 测试调试

`cpu0_main.c`

## 6. WIFI SPI 模块（上位机调试）

前备知识：计网TCP、单片机 DMA

常用的稳定波特率是 **115200**。

- **实际传输速度：** 约 11.5 KB/s。
- **传输一张图耗时：**75 KB÷11.5 KB/s≈6.5 秒。
- 需要配置 SPI 通道，且**必须使用 DMA**。
- 在 1 核（CM7）里处理图像时，如果你用 SPI 传输，图像数据可以直接从内存（SRAM）通过 DMA 搬运到 SPI 发送寄存器，**不占用 CPU 时间**

![](https://cdn.nlark.com/yuque/0/2026/png/61324111/1774355672624-2e5ffcdf-a2b3-4943-9fab-07bb981262bb.png)

【按照逐飞助手功能和wifispi例程尝试 】

- 图像传输没有图片
- 串口助手没有数据发送
- 更改热点名称无中文字符 仍然没有找到问题所在

### 6.1. debugger

1. assert 断言

【本项目的应用在于不变式检查，确保图像处理的计算稳定性和后续图像算法调试】

void assert（int expression）;

- expression 是一个条件表达式。当程序运行到**assert**函数时，会首先计算`**expression**`的值。如果`**expression**`的值为真（非零），则程序继续正常执行；如果`**expression**`的值为假（0），则**assert**函数会在 标准 错误流中输出一条错误消息，并调用**abort**函数终止程序的执行。

- 在调试阶段，**assert**函数可以帮助我们快速定位问题所在；在发布版本中，可以通过定义`**NDEBUG**` 宏 来禁用assert函数，从而避免运行时开销。

- 调试技巧：

```
void process_buffer(char* buf, size_t len) {
    // 基础检查
    assert(buf != NULL);
    assert(len > 0);
    
    // 进阶检查：缓冲区对齐
    assert((uintptr_t)buf % 8 == 0 && "Buffer should be 8-byte aligned");
    
    // 业务逻辑检查
    assert(len <= MAX_BUFFER_SIZE);

//保证内存安全
//与业务约束
//性能优化检查内存对齐情况
```

```
void update_player_state(Player* player) {
    assert(player != NULL);
    
    switch(player->state) {
        case IDLE:
            // 处理闲置逻辑
            break;
        case MOVING:
            // 处理移动逻辑
            assert(player->velocity > 0); // 移动状态速度应为正
            break;
        case ATTACKING:
            // 处理攻击逻辑
            assert(player->weapon != NULL); // 攻击必须持有武器
            break;
        default:
            assert(0 && "Invalid player state"); // 捕获未处理的状态
    }
}

//验证状态转换的合理性
```

```
//目的：
//1 .确保数据结构的完整性
//2. 捕获内存管理错误
//3. 验证算法实现的正确性

struct Vector {
    size_t capacity;
    size_t size;
    int* data;
};
 
void vector_push_back(Vector* vec, int value) {
    // 前置条件检查
    assert(vec != NULL);
    assert(vec->size <= vec->capacity);
    assert(vec->capacity == 0 || vec->data != NULL);
    
    // 扩容逻辑...
    
    // 后置条件检查
    assert(vec->size > 0);
    assert(vec->data[vec->size-1] == value);
    assert(vec->size <= vec->capacity);
}
```

```
void render_scene(Scene* scene) {
    assert(scene != NULL);
    
    #ifdef DEBUG
    // 只在调试版本执行的性能检查
    clock_t start = clock();
    #endif
    
    // 实际渲染逻辑...
    
    #ifdef DEBUG
    clock_t end = clock();
    double elapsed = (double)(end - start)/CLOCKS_PER_SEC;
    assert(elapsed < 0.016 && "Frame rendering took too long");
    #endif
}
//该技巧适合实时系统上的应用
```

```
#define ASSERT_RANGE(value, min, max) \
    ASSERT_MSG((value) >= (min) && (value) <= (max), \
              "Value " #value " out of range [" #min "," #max "]")
 
// 使用示例
void set_volume(int volume) {
    ASSERT_RANGE(volume, 0, 100);
    // ...
```

## 7. CYT4bb7 芯片双核通信

### 7.1. cross_memory_access

共享内存处，0核1核都可以访问。【理解为图像缓冲区】

PS：摄像头和屏幕初始化都是对硬件状态（芯片外设寄存器）直接设置，不用双核都进行初始化或者通信，0核内完成一次配置即可；

#### 7.1.1. 建立通信方式：

  
1. main文件当中在主函数之前进行定义和地址的声明

`main_cm7_0`:

a.#define DATA_LENGTH xx

(宏定义)

b.#pragma location = 0x2808'070c

（具体地址编译后查找.map文件）

c.__no_init float m7_1_data[DATA_LENGTH];

`main_cm7_1`:

a. #define DATA_LENGTH xx

b.#pragma location = 0x2808'070c

c. float m7_1_data[DATA_LENGTH] = {0,0.1,0.2,0.3};

2. while循环当中更新Cache

`main_cm7_0`:

a. system_delay_ms(100);

b.SCB_CleanInvalidateDCache_by_Addr(&m7_1_data,sizeof(m7_1_data));

更新cache

c.printf();

将数据打印到串口

`main_cm7_1`:

a.system_delay_ms(300);

b.SCB_CleanInvalidateDCache_by_Addr(&m7_1_data,sizeof(m7_1_data));

### 7.2. IPC interrupt

结构体封装图片数组

进行传输：

（transmit state）

标志位1 ：进入单边桥状态

标志位2：进入障碍物状态

标志位3：进入台阶跳跃状态

标志位4：进入雷区转圈状态

（transmit feature）

雷区--->传输中心坐标

台阶 --->传输台阶边缘在2D图片中的位置，测算距离

（台阶边缘到达图片下方，传输指令）

单边桥 --->？

### 7.3. Shared SRAM

- 【rt1064---TCM】

![](https://cdn.nlark.com/yuque/0/2026/png/61324111/1774942397488-ce8c5335-c40f-4240-93d2-0d8be00042ed.png)

![](https://cdn.nlark.com/yuque/0/2026/png/61324111/1774942404114-87b7c73c-db43-4f69-a0b7-5d6a42c4a5ef.png)

【CYT4bb7-----SRAM】

![](https://cdn.nlark.com/yuque/0/2026/png/61324111/1775270310043-b609df74-cc4d-4755-aceb-54bde06365af.png)

![](https://cdn.nlark.com/yuque/0/2026/png/61324111/1775270293163-0bc708ce-b553-4ad5-8d3d-b9821f81469d.png)

- 受限于芯片内存设置 ，将图像计算存放在ITCM当中似乎行不通。

![](https://cdn.nlark.com/yuque/0/2026/png/61324111/1775270353165-21968d11-da9c-4454-a38c-686f65a6fe7c.png)

![](https://cdn.nlark.com/yuque/0/2026/png/61324111/1775270360484-88185fe8-7f51-4495-b4a3-7a140c2bc44b.png)

#### 7.3.1. DMA

Direct Memoery Access，通过硬件实现的数据传输机制。

1. DMA请求
2. DMA初始化
3. DMA启动，总线出借
4. DMA数据
5. 总线归还
6. DMA停止

大量数据传输，要求使用DMA

#### 7.3.2. Cache

集成在CPU里的 极高速、容量小 的缓存

- 存在一致性问题
- 地址映射，没有独立地址
- 存在“Cache Hit”“Cache Miss”

#### 7.3.3. flash

- 速度慢、容量大的非易失性存储器（掉电不掉参）
- 需要等待周期（wait states），限制CPU性能发挥
- 存放Code和RO-data(只读常量数据)

#### 7.3.4. TCM

【紧耦合存储器】

- ITCM

instruction ！！！

---

运行中断函数、高频算法，使CPU运行不受flash限制

- DTCM

Data！！！！！！

---

存放堆栈Stack、关键变量的数据缓冲区，可极速响应

【CYT4bb7】

**TCM 的存在**：在 Traveo II 的某些配置中，CM7 核确实有紧耦合内存（TCM），但你会发现它可能被直接映射在特定的地址段（比如 `0x0000_0000` 附近）。

#### 7.3.5. SRAM

【静态随机存取存储器】----易失性存储器 掉电数据丢失

- SRAM常常被分为多块 （如 AXI SRAM, SRAM1, SRAM2 等），分布在不同的总线域上，方便 DMA 和 CPU 并发访问而不产生冲突。
- 存放变量、缓存、DMA传输数据

**启动控制**：CM0+ 是主核（Master），它负责启动 CM7。在 CM0+ 上跑通算法，意味着你已经搞定了基础的系统时钟、Flash 读取和基本的 SRAM 配置。

**硬件权限**：CM0+ 通常拥有最高的系统访问权限，调试外设（如摄像头接口或显示接口）更直接。

**架构差异**：CM0+ 是冯·诺依曼架构，没有 CM7 那样的 L1 Cache、FPU（浮点运算单元）和 SIMD（指令级并行）。

- _注意_：如果你的图像算法涉及大量 `float` 运算，CM0+ 只能通过软件模拟，速度会极慢。

Traveo II 系列非常强调 **MPU（内存保护单元）**。

- 在 CM0+ 上跑通的代码，搬到 CM7 后可能因为 MPU 没配置导致 **HardFault**。
- **调试建议**：在 CM0+ 验证阶段，尽量保持内存分配在“公共区域”（Shared SRAM），这样两个核都能看到，方便对比调试。